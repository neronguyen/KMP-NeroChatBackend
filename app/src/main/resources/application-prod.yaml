spring:
  jpa:
    hibernate:
      ddl-auto: validate

    properties:
      format_sql: false
      show_sql: false

jwt:
  expiration-minutes: 15

email:
  url: ${DEPLOY_URL}

nginx:
  require-proxy: true

rate-limit:
  ip:
    apply-limit: true
